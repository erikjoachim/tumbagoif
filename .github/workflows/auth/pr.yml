name: Auth - Pull Request Checks

on:
  pull_request:
    types: [opened, synchronize, reopened]
    branches:
      - main
    paths:
      - "auth/**"
      - "!**.md"
      - "!AGENTS.md"

concurrency:
  group: auth-pr-${{ github.head_ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build @tumbagoif/auth-server
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: package-lock.json
      - name: Install dependencies
        run: npm ci
      - name: Build @tumbagoif/auth-server
        run: npm run build --workspace @tumbagoif/auth-server

  format:
    name: Check formatting
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: package-lock.json
      - name: Install dependencies
        run: npm ci
      - name: Prettier check
        run: npx prettier --check "auth/server/**/*.{ts,json}"